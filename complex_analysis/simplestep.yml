process:
  process_type: 'string-interpolated-cmd'
  cmd: 'echo {parameter_one}'
publisher:
  publisher_type: 'frompar-pub'
  outputmap:
    step_output: parameter_one
environment:
  environment_type: 'docker-encapsulated'
  image: lukasheinrich/workflow-dummy

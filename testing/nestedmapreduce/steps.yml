stepA:
  process:
    process_type: string-interpolated-cmd
    cmd: hello
  environment:
    environment_type: noop-env
  publisher:
    publisher_type: 'constant-pub'
    publish:
      outputA: 'output'  
